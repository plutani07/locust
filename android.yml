name: Build Locust APK

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  apk:
    name: apk
    runs-on: ubuntu-latest
    steps:
      - name: Get the code
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Set up the Android SDK
        uses: android-actions/setup-android@v3

      - name: Accept SDK licences
        run: yes | sdkmanager --licenses || true

      - name: Restore the signing key
        run: |
          mkdir -p ~/.android
          if [ -n "${{ secrets.LOCUST_KEYSTORE }}" ]; then
            echo "${{ secrets.LOCUST_KEYSTORE }}" | base64 -d > ~/.android/debug.keystore
            echo "Using the committed signing key — this APK will update in place."
          else
            echo "::warning::LOCUST_KEYSTORE secret is missing. This build gets a throwaway key,"
            echo "::warning::so it will NOT install over your existing app."
          fi

      - name: Install dependencies
        run: npm install

      - name: Add the Android platform
        run: npx cap add android

      - name: Generate app icons
        run: |
          npm i -D @capacitor/assets
          npx capacitor-assets generate --android --iconBackgroundColor '#08090F' --iconBackgroundColorDark '#08090F' || echo "icon generation skipped"

      - name: Copy the web app in
        run: npx cap sync android

      - name: Build
        run: |
          chmod +x android/gradlew
          cd android && ./gradlew assembleDebug --no-daemon

      - name: Upload the APK
        uses: actions/upload-artifact@v4
        with:
          name: locust-apk
          path: android/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
